@page
@model MVCJournalApi.Pages.Lesson.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudyOccupation[0].TimeOccupation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudyOccupation[0].StudyGroup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudyOccupation[0].StudySubject)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.StudyOccupation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TimeOccupation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudyGroup.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudySubject.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
